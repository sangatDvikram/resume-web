{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && yarn install --immutable",
  "buildCommand": "cd ../.. && yarn workspace @portfolio-cms/types build && yarn workspace api build",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./ ../../packages/types ../../yarn.lock ../../package.json",
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 30,
      "includeFiles": "{dist/**,public/**,node_modules/@adminjs/**,node_modules/adminjs/**,node_modules/swagger-ui-dist/**}"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
